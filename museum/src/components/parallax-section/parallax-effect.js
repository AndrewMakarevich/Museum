export function parallaxEffect() {
    // const img = document.querySelector('.parallax-image');
    // img.addEventListener('mouseover', () => {
    //     img.addEventListener('mousemove', (e) => {
    //         let x = e.clientX;
    //         let y = e.clientY;
    //         img.style.transform = `translate(${-x * 0.01}px, ${-y * 0.01}px)`;
    //     });
    // });

}